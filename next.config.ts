import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["res.cloudinary.com", "download.logo.wine", "logos-world.net"],
  },
};

export default nextConfig;
